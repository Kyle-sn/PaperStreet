# Repository Interaction Rules

When asked about files in a directory:

1. List the files in that directory.
2. Open the files and inspect their contents.
3. Base responses only on files that exist.

Do not invent files or modules.

If a file is not visible in the repository, say so.
